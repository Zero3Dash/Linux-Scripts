(Use IPv4 Only, no IPv6 support)
sudo curl https://download.argon40.com/argon1.sh | sudo bash
argonone-config

sudo omv-salt stage run deploy
sudo omv-upgrade
sudo omv-release-upgrade
omv-upgrade

sudo omv-salt deploy run nginx phpfpm
sudo systemctl restart nginx
sudo systemctl restart php8.2-fpm
dpkg -l | grep openme

sudo wget --inet4-only -O - https://github.com/OpenMediaVault-Plugin-Developers/packages/raw/master/install | sudo bash
sudo wget --inet4-only -O - https://github.com/OpenMediaVault-Plugin-Developers/installScript/raw/master/fix6to7upgrade | sudo bash

sudo apt update
sudo apt full-upgrade
sudo reboot

sudo rpi-update
sudo reboot

wget https://github.com/mayswind/AriaNg/releases/download/1.3.7/AriaNg-1.3.7-AllInOne.zip; sudo unzip AriaNg*.zip -d /var/www/html/aria2
